[build]
command = "curl -fsSL https://vite.plus | bash && . \"$HOME/.vite-plus/env\" && vp install && vp run @schema-benchmarks/schemas#build && vp run @schema-benchmarks/website#build"
publish = "website/dist/client"
