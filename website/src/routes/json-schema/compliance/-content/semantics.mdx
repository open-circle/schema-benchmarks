Libraries that have their own representations of schemas, but can be created from JSON Schema, such as [`zod`](https://zod.dev/) or [`joi`](https://joi.dev/).

```ts
const zodSchema = z.fromJSONSchema(jsonSchema);
assert(zodSchema.safeParse(input).success === expected);
```

Results indicate how closely the library aligns with the formal semantics of JSON Schema.
