Using the [JSON Schema Test Suite](https://github.com/json-schema-org/JSON-Schema-Test-Suite), we test each library against the official JSON Schema specification. The test suite contains a large number of test cases that cover various aspects of JSON Schema validation, including different data types, formats, and validation rules.
