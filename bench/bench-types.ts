export enum DataType {
	Error = "error",
	Success = "success",
}

export enum LibraryType {
	Runtime = "runtime",
	Precompiled = "precompiled",
}

export enum AbortType {
	Do = "abort early",
	DoNot = "no abort early",
}
