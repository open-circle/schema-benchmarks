pre-commit:
  commands:
    lint:
      run: pnpm run lint --fix {staged_files}
      stage_fixed: true
    format:
      run: pnpm run format --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
